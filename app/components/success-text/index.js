import styled from 'styled-components';
import { APP_GREEN } from '../../constants/color-constants';

const StyledSuccessText = styled.div`
  font-weight: 400;
  font-size: 12px;
  line-height: 16px;
  font-style: normal;
  font-family: 'Roboto', sans-serif;
  color: ${APP_GREEN};
`;

export default StyledSuccessText;
