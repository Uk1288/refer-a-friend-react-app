import styled from 'styled-components';
import { APP_RED } from '../../constants/color-constants';

const StyledErrorText = styled.div`
  font-weight: 400;
  font-size: 12px;
  line-height: 16px;
  font-style: normal;
  font-family: 'Roboto', sans-serif;
  color: ${APP_RED};
`;

export default StyledErrorText;
