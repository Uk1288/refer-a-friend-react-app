import { defineMessages } from 'react-intl';

export const scope = 'app.components.SubHeader';

export default defineMessages({
  subheader: {
    id: `${scope}.subheader`,
    defaultMessage: 'Invite friends, get discount code',
  },
});
